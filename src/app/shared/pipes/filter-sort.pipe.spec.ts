import { FilterSortPipe } from './filter-sort.pipe';

describe('FilterSortPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSortPipe();
    expect(pipe).toBeTruthy();
  });
});
